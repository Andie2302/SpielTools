namespace SpielTools;

public enum GameAction
{
    Idle = 0,       // Nichts tun / Warten
    MoveLeft = 1,   // Nach Links laufen
    MoveRight = 2,  // Nach Rechts laufen
    Jump = 3        // Springen
}